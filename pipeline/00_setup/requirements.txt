# Core ML / NLP
torch>=2.2.0
transformers>=4.42.0
peft>=0.10.0
accelerate>=0.30.0
bitsandbytes>=0.43.0  # dla QLoRA fallback (Modal NVIDIA)
sentencepiece>=0.2.0
tokenizers>=0.19.0

# Apple Silicon LLM (mlx-lm)
mlx>=0.15.0; sys_platform == 'darwin' and platform_machine == 'arm64'
mlx-lm>=0.15.0; sys_platform == 'darwin' and platform_machine == 'arm64'

# Klasyczne ML
scikit-learn>=1.4.0
xgboost>=2.0.0
scipy>=1.13.0
numpy>=1.26.0
pandas>=2.2.0

# Data handling
datasets>=2.19.0  # Hugging Face
datasketch>=1.6.4  # MinHash dla near-duplicate detection
trafilatura>=1.10.0  # do scrapingu (już używane)
feedparser>=6.0.10  # RSS parser (Demagog, OKO.press)
beautifulsoup4>=4.12.0
requests>=2.31.0
python-dotenv>=1.0.0

# DiffBot client
# (DiffBot nie ma oficjalnego klienta — używamy requests)

# Polish NLP
# herbert-base-cased pobierany przez transformers automatycznie
spacy>=3.7.0

# Cloud compute
modal>=0.62.0

# Wizualizacja + raportowanie
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.20.0  # interactive plots dla eksploracji
tqdm>=4.66.0
rich>=13.7.0  # ładne CLI logi

# Statystyka / calibration
statsmodels>=0.14.0  # McNemar
netcal>=1.3.0  # ECE, reliability diagrams (alternatywa do własnej impl.)

# Testy / jakość
pytest>=8.0.0
ruff>=0.4.0
